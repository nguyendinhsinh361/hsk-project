import { Body, Controller, HttpException, HttpStatus, Post, Query, Req, Res } from "@nestjs/common";
import { ApiResponse, ApiTags } from "@nestjs/swagger";

@Controller('event-prize')
@ApiTags('Event Prize')
export class EventPrizeController {
    constructor(
    ) { }

}
